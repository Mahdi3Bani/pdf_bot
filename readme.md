to make this code work you need to add .env file in the server that contains the openai api key
install all requirement by pip install -r requirements.txt
navigate to server and run uvicorn app.main:app --reload
navigate to client and run npm run dev
